{
  "id": "pr-annotate-2",
  "title": "PR #2: Add optional PR learning module generation to task workflow",
  "subtitle": "KAN-93 — annotate",
  "description": "Annotated walkthrough of changes in PR #2. Adds a new optional step to the task-workflow.md that enables Kiro to generate learning modules for PRs.",
  "lessons": [
    {
      "id": "overview",
      "title": "PR Overview",
      "view": "markdown",
      "guide": "## PR #2: Add optional PR learning module generation to task workflow\n\n**Task:** KAN-93 · **Repo:** annotate · **Branch:** feature/pr-learning-module-workflow\n\n### What this PR does\n\nAdds a new optional step (step 8) to the team's task workflow process. After raising a PR, Kiro now asks the developer if they want a learning module generated. If yes, Kiro:\n\n1. Gathers the PR context (changed files, descriptions)\n2. Generates annotated walkthroughs explaining each change\n3. Pushes the module to the `annotate-modules` repo\n4. Adds a direct link to the Annotate app in the PR description\n\n### Why\n\nThis creates an automatic documentation trail for every PR. Reviewers and future developers can open the Annotate link to see not just *what* changed, but *why* — with guided explanations alongside the actual code.\n\n### Files changed\n\n| File | What changed |\n|------|-------------|\n| `.kiro/steering/task-workflow.md` | Added step 8 (learning module prompt) and renumbered step 9 |\n\n### Impact\n\nThis is a steering file change — no application code was modified. The new step applies to all repos that use this task workflow. It's fully optional (the developer can decline)."
    },
    {
      "id": "task-workflow",
      "title": "task-workflow.md",
      "view": "code",
      "file": "task-workflow.md",
      "guide": "## .kiro/steering/task-workflow.md\n\nThis is the team's standard process that Kiro follows when picking up and delivering work. It's a steering file — automatically loaded into every Kiro session.\n\n### What changed\n\nA new **step 8: Prompt for Learning Module** was inserted between the existing regression test step (7) and the task board update (renumbered to 9).\n\n### The new step\n\nAfter creating a PR, Kiro asks:\n\n> Want me to generate a learning module for this PR?\n\nIf the developer says yes, Kiro follows a 5-step process:\n\n1. **Gather PR context** — uses the GitHub API to get changed files and their content\n2. **Generate annotated content** — writes walkthrough guides for each file explaining what changed and why\n3. **Build module.json** — structures the content as an Annotate module with a markdown overview lesson and code lessons for each file\n4. **Push to annotate-modules** — creates the module folder and updates `index.json` via the GitHub API\n5. **Add link to PR** — appends an Annotate URL to the PR description\n\n### Module ID convention\n\nModules are named `pr-{repo}-{pr-number}` using flat dashes (e.g. `pr-annotate-2`). This keeps folder names simple and URL-safe.\n\n### Design decisions\n\n- **Optional** — the step is skipped if the developer says no. Not every PR needs a walkthrough.\n- **No backend needed** — Kiro writes directly to the `annotate-modules` repo via GitHub API. The Annotate app reads from there dynamically.\n- **Self-contained instructions** — the workflow file contains everything Kiro needs to execute the step, including the module.json structure template and the exact GitHub API calls to make.\n- **Renumbered steps** — the old step 8 (Update Task Board) became step 9. The \"If N\" skip from step 7 correctly points to step 8 (the new learning module step)."
    }
  ]
}